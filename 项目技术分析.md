# Nanobrowser 项目技术分析文档

## 一、项目概述

Nanobrowser 是一个开源的 AI 网页自动化 Chrome 扩展，运行在浏览器本地环境中的多智能体系统。它是 OpenAI Operator 的免费替代方案，支持多个 LLM 提供商（OpenAI、Anthropic、Gemini、Ollama 等）。

### 核心特性
- **100% 免费**：无需订阅费用，使用自己的 API 密钥
- **隐私优先**：所有操作在本地浏览器中运行，凭证不会共享
- **灵活的 LLM 选项**：支持多个 LLM 提供商，可为不同智能体配置不同模型
- **完全开源**：代码完全透明，无黑盒操作

## 二、项目架构

### 2.1 Monorepo 结构

项目采用 **Monorepo** 架构，使用以下技术进行管理：

- **包管理器**：pnpm (v9.15.1)
- **构建编排**：Turbo (v2.5.3)
- **工作空间**：pnpm workspaces

### 2.2 工作空间组织

项目分为三个主要部分：

#### 核心扩展 (`chrome-extension/`)
- **位置**：`chrome-extension/`
- **职责**：Chrome 扩展的主入口和后台脚本
- **关键目录**：
  - `src/background/` - 后台服务工作者（Service Worker）
  - `src/background/agent/` - AI 智能体实现
  - `src/background/browser/` - 浏览器自动化和 DOM 操作
  - `src/background/services/` - 各种服务（分析、语音转文字等）

#### UI 页面 (`pages/`)
- **side-panel/** - 主聊天界面（React + TypeScript + Tailwind）
- **options/** - 扩展设置页面（React + TypeScript）
- **content/** - 内容脚本，用于页面注入

#### 共享包 (`packages/`)
- **shared/** - 通用工具和类型定义
- **storage/** - Chrome 扩展存储抽象层
- **ui/** - 共享 React 组件
- **i18n/** - 国际化支持
- **schema-utils/** - 验证模式（Zod schemas）
- **dev-utils/** - 开发工具
- **hmr/** - 热模块替换支持
- **vite-config/** - Vite 配置共享
- **tailwind-config/** - Tailwind CSS 配置共享
- **tsconfig/** - TypeScript 配置共享
- **zipper/** - 打包工具

## 三、核心技术栈

### 3.1 前端技术

- **React 18.3.1** - UI 框架
- **TypeScript 5.5.4** - 类型系统
- **Tailwind CSS 3.4.17** - 样式框架
- **Vite 6.4.1** - 构建工具

### 3.2 Chrome 扩展技术

- **Chrome Extension Manifest V3** - 扩展清单规范
- **Service Worker** - 后台脚本运行环境
- **Chrome APIs** - 浏览器 API 集成
  - `chrome.tabs` - 标签页管理
  - `chrome.runtime` - 运行时通信
  - `chrome.debugger` - 调试器 API（用于 Puppeteer）
  - `chrome.sidePanel` - 侧边栏面板

### 3.3 浏览器自动化

- **Puppeteer Core 24.31.0** - 浏览器自动化核心库
  - 通过 Chrome DevTools Protocol (CDP) 连接
  - 使用 ExtensionTransport 连接到标签页
  - 支持 DOM 操作、页面导航、截图等功能

### 3.4 AI/LLM 集成

- **LangChain.js** - LLM 集成框架
  - `@langchain/core` - 核心功能
  - `@langchain/openai` - OpenAI 支持
  - `@langchain/anthropic` - Anthropic (Claude) 支持
  - `@langchain/google-genai` - Google Gemini 支持
  - `@langchain/ollama` - Ollama 本地模型支持
  - `@langchain/groq` - Groq 支持
  - `@langchain/cerebras` - Cerebras 支持
  - `@langchain/deepseek` - DeepSeek 支持
  - `@langchain/xai` - xAI 支持

### 3.5 数据验证与处理

- **Zod 3.25.76** - Schema 验证库
- **zod-to-json-schema** - Zod 到 JSON Schema 转换
- **jsonrepair** - JSON 修复工具

### 3.6 其他工具

- **webextension-polyfill** - 浏览器扩展 API 兼容层
- **posthog-js** - 分析服务（可选）
- **Vitest** - 单元测试框架

## 四、多智能体系统架构

### 4.1 智能体概述

项目实现了两个核心智能体：

1. **Navigator（导航器）** - 负责 DOM 交互和网页导航
2. **Planner（规划器）** - 负责高级任务规划和策略制定

> 注：原文档提到 Validator（验证器），但在当前代码实现中，验证功能已整合到 Planner 中。

### 4.2 Executor（执行器）

`Executor` 类是多智能体系统的协调中心：

```typescript
class Executor {
  private readonly navigator: NavigatorAgent;
  private readonly planner: PlannerAgent;
  private readonly context: AgentContext;
  // ...
}
```

**执行流程**：
1. 初始化 Navigator 和 Planner 智能体
2. 周期性运行 Planner（根据 `planningInterval` 配置）
3. 执行 Navigator 操作
4. 检查任务完成状态
5. 管理执行状态和事件

### 4.3 Navigator Agent（导航器智能体）

**职责**：
- DOM 元素识别和交互
- 页面导航（URL 跳转、前进、后退）
- 表单填写
- 元素点击
- 内容提取
- 滚动操作

**关键特性**：
- 支持多动作序列（`maxActionsPerStep` 配置）
- 视觉上下文支持（截图和边界框）
- 错误处理和重试机制
- 动作修复（修复格式错误的动作）

**动作类型**（部分）：
- `go_to_url` - 导航到 URL
- `click_element` - 点击元素
- `input_text` - 输入文本
- `scroll_to_percent` - 滚动到百分比位置
- `next_page` / `previous_page` - 翻页
- `cache_content` - 缓存内容
- `done` - 完成任务

### 4.4 Planner Agent（规划器智能体）

**职责**：
- 任务分解和规划
- 进度评估
- 挑战识别
- 下一步建议
- 任务完成验证

**输出格式**：
```typescript
{
  observation: string;      // 当前状态分析
  done: boolean;            // 任务是否完成
  challenges: string;       // 潜在挑战
  next_steps: string;       // 下一步建议
  final_answer: string;     // 最终答案（完成时）
  reasoning: string;        // 推理过程
  web_task: boolean;        // 是否为网页任务
}
```

**工作模式**：
- 如果 `web_task = false`：直接回答用户问题，无需网页导航
- 如果 `web_task = true`：规划网页浏览任务步骤

### 4.5 Agent Context（智能体上下文）

`AgentContext` 管理智能体的共享状态：

- **任务 ID**：当前任务标识
- **浏览器上下文**：BrowserContext 实例
- **消息管理器**：MessageManager，管理对话历史
- **事件管理器**：EventManager，处理事件订阅和发布
- **选项配置**：AgentOptions（最大步数、失败次数等）

## 五、浏览器自动化实现

### 5.1 BrowserContext（浏览器上下文）

`BrowserContext` 类管理浏览器标签页和页面：

**核心功能**：
- 标签页切换和管理
- 页面附加/分离（Puppeteer 连接）
- URL 导航
- 状态缓存
- 防火墙规则（URL 白名单/黑名单）

**关键方法**：
- `switchTab(tabId)` - 切换到指定标签页
- `navigateTo(url)` - 导航到 URL
- `openTab(url)` - 打开新标签页
- `getCurrentPage()` - 获取当前页面
- `getState()` - 获取浏览器状态

### 5.2 Page（页面类）

`Page` 类封装单个标签页的操作：

**Puppeteer 集成**：
```typescript
async attachPuppeteer(): Promise<boolean> {
  const browser = await connect({
    transport: await ExtensionTransport.connectTab(this._tabId),
    defaultViewport: null,
    protocol: 'cdp',
  });
  // ...
}
```

**核心功能**：
- DOM 元素查询和操作
- 页面导航
- 截图捕获
- 高亮显示（用于可视化）
- 反检测脚本注入
- 滚动控制

### 5.3 DOM 服务

**ClickableElementProcessor**：
- 识别可交互元素
- 生成元素索引
- 构建元素树结构
- 过滤不可见元素

**DOM 状态构建**：
- 构建完整的 DOM 树
- 维护选择器映射
- 跟踪元素可见性
- 处理动态内容

### 5.4 动作执行

动作通过 `ActionBuilder` 构建，支持：

1. **导航动作**：`go_to_url`, `go_back`, `search_google`
2. **交互动作**：`click_element`, `input_text`
3. **滚动动作**：`scroll_to_percent`, `next_page`, `previous_page`
4. **标签页动作**：`switch_tab`, `open_tab`, `close_tab`
5. **内容动作**：`cache_content`, `extract_text`
6. **等待动作**：`wait`
7. **完成动作**：`done`

## 六、存储系统

### 6.1 Storage 包架构

`packages/storage/` 提供 Chrome 扩展存储的抽象层：

**存储模块**：
- **base/** - 基础存储类
- **chat/** - 聊天历史存储
- **settings/** - 设置存储
  - `agentModels.ts` - 智能体模型配置
  - `llmProviders.ts` - LLM 提供商配置
  - `generalSettings.ts` - 通用设置
  - `firewall.ts` - 防火墙规则
  - `analyticsSettings.ts` - 分析设置
  - `speechToText.ts` - 语音转文字设置
- **profile/** - 用户配置
- **prompt/** - 提示词收藏

### 6.2 存储类型

使用 Chrome Storage API：
- `chrome.storage.local` - 本地存储
- `chrome.storage.sync` - 同步存储（跨设备）

## 七、国际化（i18n）

### 7.1 支持的语言

- 英语（en）
- 繁体中文（zh_TW）
- 巴西葡萄牙语（pt_BR）

### 7.2 命名规范

遵循结构化命名模式：`component_category_specificAction_state`

**组件前缀**：
- `bg_` - 后台服务工作者操作
- `exec_` - 执行器/智能体执行生命周期
- `act_` - 智能体动作和网页自动化
- `errors_` - 全局错误消息
- `options_` - 设置页面组件
- `chat_` - 聊天界面元素
- `nav_` - 导航元素
- `permissions_` - 权限相关消息

**状态后缀**：
- `_start` - 动作开始
- `_ok` - 成功完成
- `_fail` - 失败状态
- `_cancel` - 取消操作
- `_pause` - 暂停状态

### 7.3 生成机制

- 源文件：`packages/i18n/locales/**/messages.json`
- 生成器：`packages/i18n/genenrate-i18n.mjs`
- 输出：`packages/i18n/lib/**`（自动生成，不应手动编辑）

## 八、构建系统

### 8.1 Turbo 配置

`turbo.json` 定义任务依赖和缓存策略：

**主要任务**：
- `ready` - 准备任务（生成 i18n 等）
- `dev` - 开发模式（热重载）
- `build` - 生产构建
- `type-check` - TypeScript 类型检查
- `lint` - 代码检查
- `prettier` - 代码格式化

### 8.2 Vite 配置

每个工作空间有独立的 `vite.config.mts`：

**chrome-extension**：
- 输出格式：IIFE（立即执行函数表达式）
- 入口：`src/background/index.ts`
- 插件：
  - `makeManifestPlugin` - 生成 manifest.json
  - `watchPublicPlugin` - 监听 public 目录
  - `watchRebuildPlugin` - 开发模式热重载

**pages（side-panel/options）**：
- 使用 `@extension/vite-config` 的 `withPageConfig`
- 别名：`@src` 指向页面 `src/` 目录

### 8.3 构建输出

- **开发模式**：`dist/` 目录（带 sourcemap）
- **生产模式**：`dist/` 目录（压缩、无 sourcemap）
- **打包**：`dist-zip/` 目录（通过 zipper 包）

## 九、开发工作流

### 9.1 开发命令

```bash
# 安装依赖
pnpm install

# 开发模式（热重载）
pnpm dev

# 构建生产版本
pnpm build

# 类型检查
pnpm type-check

# 代码检查
pnpm lint

# 代码格式化
pnpm prettier

# 打包扩展
pnpm zip

# 端到端测试
pnpm e2e
```

### 9.2 工作空间作用域命令

```bash
# 构建特定工作空间
pnpm -F chrome-extension build

# 开发特定工作空间
pnpm -F chrome-extension dev

# 类型检查特定工作空间
pnpm -F packages/storage type-check
```

### 9.3 热模块替换（HMR）

`packages/hmr/` 提供开发时的热重载支持：
- 监听文件变化
- 自动重建
- 扩展自动刷新

## 十、安全机制

### 10.1 防火墙系统

- **URL 白名单**：允许访问的 URL 列表
- **URL 黑名单**：禁止访问的 URL 列表
- 在 `BrowserContext` 中实现 URL 验证

### 10.2 输入验证

- 使用 Zod schemas 验证所有动作参数
- URL 验证防止恶意导航
- 元素索引验证防止越界访问

### 10.3 权限管理

- 最小权限原则
- 仅在需要时请求权限
- 权限请求页面：`permission/index.html`

### 10.4 反检测机制

`Page` 类注入反检测脚本：
- 隐藏 WebDriver 特征
- 模拟真实浏览器行为
- 防止网站检测自动化工具

## 十一、消息通信

### 11.1 通信架构

**Background ↔ Side Panel**：
- 使用 `chrome.runtime.onConnect` 建立长连接
- 端口名称：`side-panel-connection`
- 消息类型：
  - `new_task` - 新任务
  - `follow_up_task` - 后续任务
  - `cancel_task` - 取消任务
  - `pause_task` / `resume_task` - 暂停/恢复
  - `screenshot` - 截图请求
  - `state` - 获取状态
  - `speech_to_text` - 语音转文字
  - `replay` - 重放历史

### 11.2 事件系统

`EventManager` 管理执行事件：
- 订阅/取消订阅机制
- 事件类型：`EXECUTION`
- 事件状态：`TASK_START`, `STEP_START`, `TASK_OK`, `TASK_FAIL` 等

## 十二、服务模块

### 12.1 Analytics（分析服务）

- 使用 PostHog 进行匿名分析
- 跟踪任务开始、域名访问等事件
- 可配置开启/关闭

### 12.2 Speech-to-Text（语音转文字）

- 支持多个 LLM 提供商的语音转文字 API
- 处理 base64 音频数据
- 错误处理和重试

## 十三、代码质量

### 13.1 TypeScript 配置

- 严格模式启用
- 共享配置：`packages/tsconfig/`
  - `base.json` - 基础配置
  - `app.json` - 应用配置
  - `utils.json` - 工具配置

### 13.2 ESLint 配置

- Airbnb TypeScript 配置
- React/Hooks 规则
- Import 排序规则
- 可访问性规则（A11y）
- 强制使用类型导入：`@typescript-eslint/consistent-type-imports`

### 13.3 Prettier 配置

- 2 空格缩进
- 单引号
- 分号
- 尾随逗号
- 行宽：120 字符

### 13.4 测试

- **框架**：Vitest
- **位置**：`chrome-extension/src/**/__tests__/*.test.ts`
- **运行**：`pnpm -F chrome-extension test`

## 十四、部署与分发

### 14.1 构建流程

1. 运行 `ready` 任务（生成 i18n 等）
2. 构建所有工作空间
3. 输出到 `dist/` 目录
4. 生成 `manifest.json`

### 14.2 打包流程

1. 运行构建
2. 使用 `zipper` 包打包
3. 输出到 `dist-zip/` 目录

### 14.3 分发方式

- **Chrome Web Store**：稳定版本
- **GitHub Releases**：最新版本（手动安装）

## 十五、技术亮点

### 15.1 多智能体协作

- Navigator 和 Planner 分工明确
- 周期性规划确保任务方向正确
- 实时状态反馈和调整

### 15.2 浏览器自动化

- 使用 Puppeteer Core 通过 CDP 连接
- 无需外部浏览器进程
- 直接在扩展中运行

### 15.3 灵活的 LLM 配置

- 支持多个 LLM 提供商
- 可为不同智能体配置不同模型
- 支持本地模型（Ollama）

### 15.4 Monorepo 架构

- 代码复用性强
- 独立构建和测试
- 统一的开发体验

### 15.5 类型安全

- 全面的 TypeScript 类型定义
- Zod schemas 运行时验证
- 类型安全的存储抽象

## 十六、项目依赖关系图

```
nanobrowser (root)
├── chrome-extension (核心扩展)
│   ├── @extension/storage
│   ├── @extension/i18n
│   ├── @extension/shared
│   ├── @langchain/* (多个 LLM 提供商)
│   └── puppeteer-core
├── pages/side-panel (侧边栏)
│   ├── @extension/storage
│   ├── @extension/i18n
│   ├── @extension/shared
│   └── @extension/ui
├── pages/options (设置页)
│   └── (类似 side-panel)
└── packages/* (共享包)
    ├── storage → (无依赖)
    ├── i18n → (无依赖)
    ├── shared → (无依赖)
    └── ui → @extension/shared
```

## 十七、未来发展方向

根据项目文档和代码结构，可能的改进方向：

1. **更多 LLM 提供商支持**
2. **增强的视觉理解能力**
3. **更智能的错误恢复机制**
4. **性能优化**（减少 API 调用）
5. **更多浏览器支持**（Firefox、Safari）
6. **插件系统**（扩展动作类型）

## 十八、总结

Nanobrowser 是一个架构清晰、技术先进的 AI 网页自动化项目。它采用现代化的 Monorepo 架构，实现了灵活的多智能体系统，支持多个 LLM 提供商，并提供了完整的浏览器自动化能力。项目的代码质量高，类型安全，易于维护和扩展。

**核心优势**：
- ✅ 完全开源和免费
- ✅ 隐私优先（本地运行）
- ✅ 灵活的 LLM 配置
- ✅ 强大的浏览器自动化能力
- ✅ 良好的代码组织和类型安全

**技术栈选择合理**：
- React + TypeScript 提供类型安全的 UI 开发
- Vite 提供快速的构建体验
- Turbo 管理 Monorepo 构建
- Puppeteer Core 实现浏览器自动化
- LangChain.js 统一 LLM 接口

这是一个值得学习和参考的优秀开源项目。

